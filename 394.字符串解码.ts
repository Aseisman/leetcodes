/*
 * @lc app=leetcode.cn id=394 lang=typescript
 *
 * [394] 字符串解码
 */

// @lc code=start
function decodeString(s: string): string {
  const numStack: number[] = [];
  const stack: string[] = [];
  let i = 0;
  while (i < s.length) {
    if(s[i])
  }
}
// @lc code=end
